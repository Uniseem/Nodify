# Nodify 功能对照与后续交付

更新：2026-09-09。对照入口为[妙妙屋 X 文档](https://miaomiaowux.com/docs/)。本文记录当前实现、证据和缺口，不能用页面或接口的存在代替实际验收。

## 最新进展：Cloudflare Tunnel 发布向导与域名隔离

- HeroUI 系统设置新增本地管理的 Tunnel 向导，生成双域名路由、Docker 覆盖文件、专属 systemd 单元、固定版本/校验的安装命令与 DNS/验证命令。新增离线 `tunnel-config` 构建入口，解决新实例尚无 HTTPS 入口时不能打开向导的问题；输出与页面同源，拒绝覆盖已有目录。
- 独立发布域名持久化并带版本检查，可恢复环境地址。Agent 安装命令使用面板域名；成员分配、创建、撤销、列表、订阅文件/短链、私有页客户端链接使用订阅域名。域名设置随加密备份恢复，外部 Cloudflare 凭据不收集、不在普通接口或备份中。
- 生成的边缘规则为订阅域名限制路径并固定回源 Host；主控再次拒绝该域名的管理 HTTP 路由和 WebSocket 升级，原生部署支持显式回环监听。真实 cloudflared 2026.8.3 校验两种配置和路径匹配；真实 HTTP、JWT/域名流程与构建产物证据见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。
- 完整镜像检查发现并修复两处旧模块冲突：高级 SSH 网关不再拒绝 Agent WebSocket 路径；旧订阅 API 与响应规则移至 `/api/legacy-sub`，避免拦截 Nodify 的 `/api/sub` 权益令牌。共存测试同时加载真实旧控制器和中间件路由。
- 另发现旧高级配置编辑器所需 `wasm_exec.js` / WASM 资源尚未纳入统一构建，相关高级页面仍有缺口。Nodify 新配置编辑与私有页主应用使用各自的现有实现，不能据其通过而宣称旧编辑器可用。
- 最终 VPS 三应用类型/构建和 **99/99 测试通过，无跳过**；完整新镜像验证匿名私有订阅、管理员鉴权、两个 WebSocket 入口共存及实际 cloudflared 配置校验。Windows 80 项通过、19 项由 Linux 执行。本轮未重建裸机发行包，0.3.0 仍未冻结。
- 正式域名、DNS、Cloudflare 账户侧 Tunnel 建立和真实客户端公网更新仍需授权资源，尚未验证。远程管理 Token 模式、内置 Healthy 检查和完整 Tunnel 升级生命周期仍有缺口，不能将文件生成视为已建立公网服务。

## 最新进展：Agent 自动连接回退

- auto 按 WebSocket → 显式配置的 HTTPS 直连 → HTTPS 拉取选择通道；未配置直连凭据时保留 WebSocket → 拉取。自动模式下 WebSocket 请求超时 10 秒、直连超时 5 秒，直连失败冷却 15 秒；WebSocket 以 5–60 秒退避重连，恢复后重新优先使用。显式 ws/pull/direct 不跨通道切换。
- HeroUI 显示 Agent 已上报的策略、实际心跳通道和直连待用状态，提供四种模式的配置复制；复制只生成配置，不会提前声称 Agent 已应用。裸机首次安装支持 auto 加显式直连凭据，仍校验监听和 TLS。
- VPS 全套 **94/94，无跳过**。真实 TLS Agent 完成 WebSocket → 直连 → 拉取 → 直连 → WebSocket；同一 PTY 会话保留环境，重放任务只写入一个字节。真实 VLESS 下载计量入库后主动丢弃确认，跨通道重报不重复扣量；Agent 重启后的持久化重报仍去重。直接请求超时后的旧确认不能结束新请求。
- Windows 本轮未设置可选内核路径，**76 项通过、18 项跳过**，相应实际内核和 Linux 测试全部由 VPS 执行。手机/桌面浏览器连接隔离 VPS，核对自动状态变化、配置复制、深浅色和宽度；测试夹具不代替生产管理员鉴权与完整安装生命周期。详见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。

## 此前进展：主控到 Agent 的 HTTPS 直连

- 增加显式 direct 模式，由主控主动访问 Agent HTTPS 接口，注册之后不再由 Agent 发起管理连接；同时提供 auto / ws / pull 环境配置。当时 auto 为 WebSocket → HTTPS 拉取；其后扩展见上节。Agent 的任务执行、协议统计和 PTY 均复用现有通道处理逻辑。
- HeroUI 服务器详情提供 HTTPS 地址、独立凭据生成/复制、保存/重新载入及实际心跳通道。地址和凭据经共享契约校验；连接配置加密保存，普通接口不回显凭据。编辑使用版本检查，冲突和网络失败保留输入；断网后显示无法刷新，状态恢复不覆盖未保存内容。
- Agent 默认回环 HTTP 供 HTTPS 代理使用，也支持自身 TLS；公开监听缺少证书会拒绝启动。主控校验证书和名称、不跟随重定向，消息大小及等待时间受限；数据库租约避免重复调度，旧响应重试前再次校验身份，服务器撤销同时禁用直连。
- Linux 完整回归 **92/92，无跳过**；Windows **85 项通过、7 项需 Linux**。VPS 实际 Agent 验证注册后仅使用直连管理、凭据轮换、任务重放只执行一次、PTY、受管 VLESS 下载 64 KiB 与真实用量，重启后重放同一流量批次不重复扣量。隔离 TLS 测试覆盖错误凭据、跨服务器身份、不可信证书与同时调度。具体源码、页面和产物证据见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。

## 最新进展：服务器交互终端

- 服务器详情新增 HeroUI 交互终端，复用 Agent 的鉴权 WSS / HTTP 备用通道；真实 Linux PTY 保留目录、环境、前台程序与终端尺寸，支持中文、Ctrl+C、Tab 和手机输入框。原一次性命令入口保留。
- 新增会话及逐帧输出持久化，输入/输出加密，不进入普通任务日志。开启操作复用任务 ID；输入、输出均按序确认并去重，短暂断线后重传。每台服务器最多两会话，页面默认十分钟、输出上限 1 MiB；页面或主控失联后由 Agent 本地租约关闭，过期会话不会自动重启。
- Go PTY 辅助程序仅清理自身 Bash 会话的进程；子进程回收器处理孤儿任务，Agent 镜像增加 tini。Docker 与裸机包包含辅助程序及其许可证，缺少该能力的 Agent 在页面提示升级。Shell 不继承 Agent 注册令牌等应用环境变量，但以 Agent 系统身份运行，属于管理员操作。
- Windows 全套 **82 项通过、6 项需 Linux**；Debian 12 / amd64 VPS 最终 **88/88，无跳过**，三应用类型检查与构建通过。测试覆盖真实 WSS/HTTP Agent、隔离鉴权、去重、输出限额、窗口缩放、Ctrl+C、退出清理和 Agent 强制终止；进程检查要求 PID 消失。手机/桌面浏览器测试连接真实 VPS PTY，详细边界见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。0.3.0 仍未冻结，其他平台与完整发行生命周期须另行验收。

## 最新进展：入站传输参数与订阅一致性

- HeroUI 入站向导新增 WS/XHTTP Host、WebSocket 心跳、TLS ALPN、XHTTP 四种模式。与高级 JSON 共用本地契约；编辑保留未知字段和其他传输配置，清空可选字段恢复内核默认。Host 限定 ASCII 域名或 IPv4、不带端口；固定 Xray 的 Host 校验不接受配置值中的端口。WS ALPN 限定 http/1.1，gRPC 须包含 h2 或留空。
- 受管订阅同步 Host、ALPN 和 XHTTP 模式；模式写入 Mihomo 实际读取的 `xhttp-opts.mode`。外部 URI 导入同样保留 Host、ALPN 和模式，拒绝错误的 ALPN 数组。sing-box 过滤 XHTTP，Mihomo 1.19.30 过滤 VMess/Trojan + XHTTP，私有页列明原因。VMess URI 扩展字段的 GUI 客户端支持仍未验收。
- Xray 编译时让高级 `streamSettings.method` 别名与受管 network 保持一致，避免别名覆盖向导选择；原草稿未知字段仍保留。保存、发布失败和 Agent 确认继续分别对应草稿、原生效配置与新生效配置，节点 UUID 保持不变。
- Windows 全套 **81 项通过、4 项需 Linux**；VPS 首轮完整回归 **85/85，无跳过**，三应用类型检查与构建通过。真实 Xray 26.7.28 服务端下，sing-box 1.14.0/Mihomo 1.19.30 完成三类 TLS WS，Xray/Mihomo 完成四种 TLS XHTTP，共 14 次下载并验证用户统计；错误 Host、证书域名和模式被拒绝。随后加入 URI 导入往返的真实 Mihomo 路径，最终再次 85/85，三应用类型检查与构建通过。
- 浏览器连接真实配置服务和隔离 SQLite，验证错误输入保留、JSON 字段保留、草稿持久化、传输切换、清空恢复默认；桌面 1270/1270、手机 380/380，无横向溢出。测试夹具直接调用服务，不代表生产鉴权、公网协议或 GUI 客户端导入验收。当前源码仍为未冻结的 0.3.0 候选。

## 最新进展：模板规则集选择与文件继承

- 命名模板可附加全部启用规则集、指定规则集或不附加规则集。主订阅使用默认模板的选择；订阅文件新增“跟随当前模板”，也可明确覆盖为全部、指定或不附加。现有文件的选择不改变，新建文件界面默认跟随模板；API 省略字段仍沿用原来的全部范围。
- 新增模板与规则集的 SQLite 关联及迁移，保存模板和关联在同一事务内检查版本与资源存在性；被引用规则集不能删除，删除模板级联清理关联。停用的规则集不输出，选择顺序不改变全局规则优先级；切换默认模板只影响跟随默认的入口。
- HeroUI 模板和文件共用规则选择组件，支持停用/空选择提示、刷新可选规则且保留草稿、复制模板保留选择、中文网络错误及 15 秒超时。预览展示实际附加的启用规则与双格式规则，并修复手机端预览撑宽页面的问题。
- Windows 全套 **78 项通过、4 项需 Linux**；VPS 基于 R7 构建环境覆盖本轮源码后 **82/82，无跳过**，包括实际 Mihomo/sing-box 分流与真实 GeoIP 加载，以及加密恢复、关联删除保护、文件覆盖、排序、启停、过期和版本冲突。浏览器连接实际控制器与隔离 SQLite 完成模板保存、并发冲突保留、文件继承/覆盖及超时恢复检查。发行归档尚未重新冻结，公网 GUI 客户端验收仍独立跟进。

## 已验证基础：VPS 生命周期、页面及恢复后流量补报

- SSH 已恢复。R5 从本仓库源码在 Debian 12 / amd64 构建三应用、Docker 主控/Agent 和裸机包；Linux **78/78**、类型检查及归档/逐文件校验通过。Docker 新实例六协议、套餐撤权、备用通信、备份恢复，以及主控/Agent systemd 安装、重启、0.2.0 → 0.3.0 → 0.2.0、卸载保留重装均通过。独立 Agent 监督升级、错误校验值拒绝、升级中断回退及恢复启动失败自动回退也通过。
- 真实独立 MinIO 和 Nginx WebDAV 完成两次定时备份、认证回读与解密、远程/本地保留策略及存储重启后读取。测试没有使用或修改 VPS 原有存储；具体 AWS/R2 等云服务商仍未验收。
- R2 和 R5 临时运行环境已清理，原 Nginx/Xray/mmw-agent PID、原 Nodify 数据/环境文件和冻结 0.1.0/0.2.0 包校验保持一致。完整日志、版本与源码摘要见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。下方 SSH 中断和本地专项未部署的描述均为历史状态。
- R6 修正内部组/配置档案 SQLite 查询及原始 JSON 字段名保留；手机/桌面主入口统一，语言和退出改用 HeroUI。Windows 75 项、Linux 79/79、三应用构建/类型检查和正式接口通过，实际登录、手机导航、旧用户页、主题及退出复核完成。
- 页面额外发现重复恢复旧快照后，备份后下发的计费策略丢失，导致一批真实流量持续被拒收。R7 已加入绑定服务器与策略的认证加密计费凭据，验证后按历史计量口径入账，并修正非批次唯一约束异常被错误确认的问题。Windows 76 项、Linux **80/80**、三应用构建/类型检查和两包完整性通过。VPS 实际停止主控后仍通过 AnyTLS 下载，再恢复缺少该策略的旧备份：积压批次恰好持久化一次，队列与心跳重试错误归零。备份后已确认且已移出 Agent 队列的数据仍需较近备份或原主控数据，不能凭此重建全部快照后历史。**0.3.0 未冻结，完整目标尚未完成**。

## 历史进展：WireGuard 出站表单与实际连接

- WireGuard 出站新增 HeroUI 专用表单：私钥、多 peer、公钥/预共享密钥、IP/CIDR、allowedIPs、端点、心跳、MTU、保留字节、解析策略和 TUN 模式。新建默认使用用户空间，不修改宿主路由；已有配置未指定的字段保持未指定。修改保留未知字段，peer 可排序及通过 HeroUI 弹窗移除。
- 共享契约检查密钥格式、重复公钥（含 Base64/十六进制等价表示）、地址、端点、数值与数组结构，拒绝 WireGuard 不支持的 streamSettings。配置仍走保存草稿、发布、Agent 确认流程；失败不改变实际生效版本，任务参数加密。
- Windows 实际运行三个 Xray 进程：VLESS 客户端 → 受管 VLESS 入站/WireGuard 出站 → WireGuard peer。经用户空间隧道完成 344,064 字节 TCP 下载和 1,024 字节 UDP 回传，并验证受管用户上传/下载统计。接收端将独立隧道目标映射到隔离回环测试服务；未关闭 gVisor 的回环地址保护，未配置系统 TUN 或连接公网 VPN。
- 新增 3 项测试；全套 Windows **74 项通过，4 项需 Linux**。UI 在实际配置服务和隔离 SQLite 中验证重复公钥/MTU 错误、多行逐字输入、peer 排序、JSON 字段保留、删除取消/确认和版本持久化；手机文档 380/380，桌面 1270/1270。测试入口绕过生产鉴权，不代表服务器实际部署检查。
- 此项提供已有 WireGuard 端点的配置与连接，**WARP 注册、账户刷新、许可升级、卸载注销及公网双栈尚未实现/验收**。VPS 仍 SSH 握手超时，最新代码尚未部署；R5 源码候选继续包含此前 R4 备份改进，正式版本仍未冻结。
- 同时纠正对照表：源码及订阅文件测试已证明按文件选择规则集和删除引用保护；尚缺的是模板级独立绑定及更多规则资源导入，不应重复实现已有文件绑定。

## 最新进展：远程备份完整性与表单

- WebDAV/S3 上传后通过 HTTPS 流式读回加密文件，检查字节数和 SHA-256；超过本地文件大小立即中止，上传与回读共用 120 秒截止及任务取消信号。读回成功后才执行保留策略，远端校验或删除失败时保留本地备份；重试复用原备份 ID 和加密包。
- 备份契约移到本地共享包，拒绝地址内凭据、查询参数、片段、空白和控制字符；修正 S3 签名路径中保留字符的编码。请求禁止重定向，不把服务商响应正文或请求地址写入任务错误。
- HeroUI 表单提供本地、WebDAV、S3 的实际字段、加载/失败重试、中文校验及保存状态。重新载入保留当前存储类型，保存后清空凭据；界面说明读回校验需要读取权限并产生下载流量。
- 新增 6 项真实 HTTPS 协议夹具测试，包括 S3 请求签名验证、密文完整性、截断/超长/损坏、读取拒绝、重定向和中途取消；SQLite 集成验证失败保留、删除拒绝及重试。全套 Windows **71 项通过，4 项需 Linux**；三应用构建和类型检查通过。隔离数据库 UI 验证 WebDAV/S3 保存、刷新及错误修正，手机文档 381/381、桌面 1280/1280。UI 夹具绕过生产鉴权，临时页面和监听已清理。
- 这些是本地服务协议与数据流程证据，**尚未完成真实 WebDAV 服务或 S3 服务商验收**。VPS SSH 仍在握手阶段超时，未部署本轮改动；R3 源码归档不含这些更改，后续应从更新的 R4 候选构建。先前 R2 临时运行环境清理及 0.3.0 裸机生命周期仍待恢复连接。

## 最新进展：0.3.0 候选发布与入口修复

当前源码版本为 **0.3.0**，正式发布尚未冻结。此前新增的网站文件、订阅别名、协议/网卡账本、周期校准、首页统计及 Xray 系统/DNS 表单已经进入候选构建；冻结的 0.1.0、0.2.0 归档保留。

- 根版本生成器同步主控与 Agent 的 Compose 标签，修复标签仍停留在 0.1.0 的问题；Valkey 镜像固定为已验证存在的 9.0.0-alpine。构建与恢复文档去掉过时的固定备份版本描述。
- 首次通过真实部署的完整登录入口检查，发现登录页和外层标题仍用上游品牌，顶部版本取自上游元数据。默认名称已统一为 Nodify；自定义品牌保留。新的 HeroUI 版本弹窗分别显示本地前端构建版本和主控版本，失败可重试、版本不同有提示；查询有 15 秒超时。默认顶部不再提供上游推广/更新入口，现有高级功能入口保留。
- R2 候选在 Debian 12 / amd64 从完整源码构建，Linux 全套 **69/69**；Windows **65 项通过，4 项在 VPS 实测**。新 Docker 实例的管理员、Agent 握手、六协议受控下载、配置回退、AnyTLS 禁用/超额/到期撤权、SSE 和 HTTPS 备用通道通过。加密备份恢复还验证 DNS/策略字段、实际生效版本、协议与网卡账本、校准审计和首页设置。
- 独立 Docker 0.2.0 → 0.3.0 → 0.2.0 流程通过：升级保留管理员和服务器记录，写入新设置并重启；停写、检查点和快照回退恢复旧表结构与旧管理员。此项使用运维命令协调容器，不代表已提供自动 Docker 升级回退按钮。
- 完整手机页面检查发现固定宽度登录表单超出视口，以及旧兼容侧栏关闭后仍占布局、链接未按路由渲染的问题。本地 R3 修复为自适应登录容器、明确的连接中/失败状态和 HeroUI 导航弹窗；路由入口使用真实链接，子菜单使用独立可展开结构。通过当前源码与真实 VPS 鉴权接口验证登录、入口跳转、版本读取；修复后登录文档 390/390，无横向溢出，关闭导航后不再渲染旧侧栏。R3 本地三应用构建、类型检查及 Windows 65+4 项回归完成。
- **尚未完成：**R3 最新源码的 Linux 产物、0.3.0 裸机安装/升级/回退/恢复、独立 Agent 升级中断，以及最终包冻结。VPS 后续 SSH 握手超时，HTTP 探测也未收到响应；R2 临时容器仍待连接恢复后清理，不能把最后一次服务状态检查当作当前在线状态。详见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。

以下各项保留原阶段证据，其中“尚未进入发行包”的历史描述应结合上面的候选发布状态阅读。公网 GUI 客户端、真实 DNS ACME、WebDAV/S3、其他发行版和 arm64 仍不因此视为已验收。

## 最新验收：结构化 DNS 与 Hosts 管理

沿用[系统配置](https://miaomiaowux.com/docs/xray-system-config/)中的 DNS 功能，具体行为依据 [Xray DNS 文档](https://xtls.github.io/config/dns.html)与内核配置实现。使用现有配置版本和发布通道，无新增数据库迁移。

- HeroUI 按原顺序管理简单地址与高级 DNS 对象，支持新增、编辑、上下移动和删除确认；简单地址可显式升级为高级设置，其他记录和未知字段保留。高级表单包含端口、超时、域名优先匹配、期望/排除 IP、回退选择、finalQuery、查询策略、ECS、路由标识及缓存选项。UDP 端口可独立设置，TCP/DoH 等 URL 的端口在地址内填写。
- 全局提供查询策略、禁用缓存、过期缓存/TTL、禁用回退、命中后禁用回退、并行查询和读取系统 Hosts。内置 DNS 由使用 UseIP 的出站、IPIfNonMatch/IPOnDemand 等相应路由查询或 DNS 出站调用，并不修改宿主系统解析器，也不保证所有连接自动使用它。
- 静态 Hosts 可新增、改名、编辑单地址或地址列表及删除，重名拒绝覆盖；未写入条目阻止保存整个系统草稿和切换 JSON，可取消单条编辑。多行域名/IP 输入保留换行及输入位置。服务器排序不会转换或丢失未编辑高级对象；旧 expectIPs/clientIP 等写法在未编辑时保留，编辑相应字段才统一写法。
- 共享契约检查 DNS/Hosts 结构、布尔值、端口/超时/TTL、ECS IP 和地址格式，防止表单崩溃或无效字段流入发布。域名/IP 规则的完整解析及所需 geosite/geoip 文件仍由目标内核校验。Hosts 动态键使用自身属性检查及安全对象构造，重名或改名不会覆盖其他映射。
- VPS 全套 **69/69，通过，无跳过**；Windows **65 项通过，4 项在 VPS 实测**。真实 Xray 经本地 UDP 上游及 SOCKS 连接验证 Hosts 别名不查上游、域名优先级、期望/排除 IP 后的回退、skipFallback、disableFallbackIfMatch、finalQuery 及单服务器禁用缓存覆盖全局缓存。上游请求顺序和数量均有断言；加密恢复检查混合列表顺序、Hosts 数组、回退设置及未知字段保留。
- 浏览器通过真实配置服务和独立 SQLite 验证逐字输入第二行、端口错误后修正、服务器排序、Hosts 未完成输入保护、重名拒绝、改名和删除取消/确认；保存内容重新从 SQLite 读取核对。固定 390px 手机容器内文档宽度 380px，展开高级表单无溢出；1280px 浅色桌面布局已检查。
- 实际解析证据限于受控 UDP DNS 和上述模式；DoH/DoQ/本地模式公网链路、ECS 对外发送、系统 Hosts、并行/过期缓存、多架构及全部资源规则尚未据此宣称验收。浏览器回环入口绕过生产鉴权，不等于公网管理员流程。冻结 `0.2.0` 包未覆盖，新源码仍需发行生命周期验证。

## Xray 连接策略与连接观测表单

对应[系统配置](https://miaomiaowux.com/docs/xray-system-config/)及 [Xray 本地策略](https://xtls.github.io/config/policy.html)、[连接观测](https://xtls.github.io/config/observatory.html)。沿用服务器配置版本、发布任务和 Agent 确认流程，本轮没有新增数据库实体。

- HeroUI 系统编辑支持策略等级新增/切换/移除、握手和空闲超时、单向关闭等待、每请求缓冲区、内核在线计数及可选出站上下行统计。留空使用内核缺省值，显式零保留；未修改等级、未知字段及高级 DNS 对象不被覆盖。等级 0 固定用于受管入站用户，其他等级不会自动分配套餐。
- 用户上下行、入站上下行及 StatsService 本地 API 仍由发布器管理，界面展示实际固定设置及高级 JSON 的覆盖范围。出站诊断计数不会额外加入用户配额。AnyTLS 独立 sing-box 计量不使用这些 Xray 策略。
- 支持后台和突发观测的出站前缀、地址、时长、并发、采样、本地连通性检测和 HTTP 方法；高级 JSON 的合法自定义方法会保留。匹配结果及不足 10 秒的突发间隔执行行为有说明。切换观测方式明确替换另一种配置，取消系统编辑可恢复原值；均衡器需要的前缀在写入草稿时补齐。
- 共享契约拒绝无效策略结构、负超时、缓冲区乘以 1024 后可能溢出的值、无效时长/URL/HTTP 方法、双观测器及失效前缀。单一精确观测引用随出站改名更新；会改变其他出站匹配范围的改名须先调整选择器，删除最后一个被匹配出站不能留下失效引用。
- 输入尚未添加的等级时，写入和切换 JSON 被阻止，避免丢弃输入。等级移除提供取消和确认；校验失败保留字段、聚焦错误，继续修改即清除旧错误。浏览器通过真实配置服务及独立 SQLite 保存、读取验证未知策略字段和高级 DNS 保留。固定 390/1280px 容器内文档 380/1270px 均无横向溢出，深色手机和浅色桌面表单已检查。
- VPS 全套 **67/67，通过，无跳过**；Windows **63 项通过，4 项在 VPS 实测**。新增真实 Xray 测试验证两种观测配置可加载、管理统计不被用户关闭，并通过两个受控 HTTP 出口和实际 VLESS/SOCKS 下载验证低延迟选择、单出口故障切换、全部故障走备用及恢复重选；同时读取真实用户和出站计数。SQLite 测试验证草稿不提前生效、确认后更新及失败保留上一版本。
- 实际故障切换证据限定后台观测加 leastPing，不据此覆盖突发观测的随机调度、leastLoad 实流量、所有协议/UDP 或全部超时行为。在线计数开关不等于在线设备管理。冻结 `0.2.0` 包未覆盖，本轮源代码仍待新版本发行生命周期验收。

## 首页容量汇总与 30 日趋势

对应[流量统计口径](https://miaomiaowux.com/docs/traffic-accounting/)的管理首页统计。新增 `20260908120000_overview_traffic` 及本地共享输出契约，管理员通过 `GET /api/overview/traffic` 和 `PUT /api/overview/traffic-settings` 读取、设置。

- 服务器可选择是否计入首页；仅改变统计范围不重建对账口径，也不丢失校准。首页有限额项目分别显示已知容量、用量、账面剩余和超额；不限量、容量未知单列，缺失用量保持未知。余额逐项扣减并截为零后汇总，某项目超额不会抵消另一项目余额。不同服务器可有不同账期，界面明确这不是同一周期的账单。
- 可显式纳入启用的外部订阅，首页固定按上游上传加下载计算，不受订阅文件方向设置影响。已到期、未及时更新和记录有缺口都有提示；这些数值仅为已知快照，不保证可用额度。读取接口不包含来源 URL、密钥或 Agent 凭据。
- 30 日 UTC 趋势只读取被选服务器的日账本，包含不限量服务器；按当前数据源、接口及每日方向重算，同一服务器先合并接口再取上下行较大值。不混入手工校准或外部快照，不把空白日期画成零或跨缺口连线。实际零记录、缺少接口、跨日间隔和接收日期归档分别展示；字节汇总使用 BigInt 和十进制字符串。
- HeroUI 首页卡片支持刷新、统计设置、日期选择、明细筛选和服务器/来源管理链接。设置用版本校验防止覆盖，冲突保留选择并可重新读取服务器最新版本；后台更新发现旧草稿仍提示冲突。加载失败保留上次有效数据，保存失败保留草稿并聚焦中文错误。
- VPS **63/63，通过，无跳过**；Windows **59 项通过，4 项在 VPS 实测**。覆盖校准不进趋势、网卡合并后取较大值、未知/不限/到期外部源、版本冲突、空选择、真实零与缺失日期、超过 39 位的汇总值，以及加密恢复后保留首页设置、服务器统计范围和原校准。三应用构建、类型检查通过。
- 浏览器通过真实控制器与独立 SQLite 检查双页面冲突、重新载入、到期提示、筛选空状态、管理链接和断连保留。固定 390/1280px 容器内文档宽度为 380/1270px，均无横向溢出，桌面四列、手机单列。测试入口绕过生产管理员鉴权，测试数字通过真实服务写入；不等于全站公网认证或所有客户端 GUI 验收。
- 这部分仍是已收到的账本汇总。精确日界分摊、节点/入站维度、跨服务归因、历史保留和大规模压力验证继续保留在后续清单。冻结 `0.2.0` 发行包未被覆盖，新源码尚需随新版本完成发行生命周期验收。

## 服务器周期、校准与手工基线

对应[流量统计口径](https://miaomiaowux.com/docs/traffic-accounting/)的服务器周期及调整量。原始账本、服务器对账和用户套餐计量保持独立。

- 新增 `20260908110000_traffic_accounting`。服务器可设置 UTC 每月重置日 1–31，短月份取月末；未设置时沿用接入统计以来的累计。周期由查询时间确定，跨月后立即使用新范围及新对账记录，不依赖调度器恰好在零点运行。月周期原始量按采集归档日期汇总，旧周期补报不进入新周期。
- 支持校准到目标字节、建立零用量基线和清除手工调整。归零保存当前已收到的上下行基线，不删除日账本或修改内核计数器；最大值模式在扣除两个方向的基线后计算。界面同时展示规则原始量、基线调整、手工校准、总调整及最终用量，满足原始规则量加总调整等于计费用量。
- 校准针对当前已经收到的记录；同周期后续收到的增量，包括离线补报，仍会增加已知用量。无原始记录时可以明确手工设定用量，界面说明其来源；清除后回到未知，不把缺失数据当成零。
- 修改数据源、网卡、方向或重置日会建立新统计口径，保留旧口径的校准与基线历史；仅修改容量保留当前调整。新月不继承旧月的手工调整。服务器卡片与订阅文件管理员汇总使用同一当前用量计算；日趋势仍只显示原始增量，不混入调整量。用户套餐额度、计费和重置不受这些操作影响。
- 新管理员接口 `GET/POST /api/servers/:id/traffic-accounting` 返回当前对账视图及分页历史。操作要求设置版本、周期标识、对账版本和请求 ID，保存原因及完整前后数值；相同请求可重试，不重复应用。审计写入失败会回滚对账变更，不能只更新用量而没有记录。
- VPS 全套 **61/61，通过，无跳过**；Windows **57 项通过、4 项在 VPS 实测**，三应用构建与类型检查通过。覆盖短月/闰年/跨年、旧周期补报、最大值基线、容量与口径变化、过期操作拒绝、请求重放、无记录校准与清除、审计失败回滚及历史分页。加密恢复验证校准、基线记录和审计内容保留。
- HeroUI 通过实际控制器与独立 SQLite 检查原因必填、双页面版本冲突、保留输入并重新准备、校准/归零/清除、四条前后记录、每月 31 日设置及窄屏布局；断连后输入保留并聚焦中文错误。该回环测试入口绕过生产管理员鉴权，只用于交互验收。
- 本轮限定 UTC 月周期，没有实现任意账期或时区。精确日界分摊、节点/入站维度、跨服务归因、大规模并发和保留策略仍在后续清单；当前记录只能说明已收到的数据，不声称全天完整或与云厂商账单自动一致。冻结 `0.2.0` 包尚未包含这些改动。

## 系统网卡账本与数据源选择

- 新增 `20260908100000_network_traffic`，网卡累计和 UTC 日账本按服务器、网卡分别保存。Agent 从 Linux `/proc/net/dev` 读取计数器，以 boot ID、ifindex、时间和上下行计数判断连续性；首次采集、开机变化、网卡重建或计数回退只建立新基线，不导入之前的累计值。
- 采集基线及待确认增量一起原子落盘。独立 `POST /api/agent/network` 和 WSS `network` 消息复用 Agent 自身鉴权，主控按服务器/会话/序号去重，累计、日账本和确认同事务提交。Agent 只在序号匹配的确认后删除批次；进程重启保留原批次身份和采集时间。
- HeroUI 服务器详情可选协议用户计数器或系统网卡，网卡源必须显式选择至少一张接口。切换数据源或网卡会重新汇总已采集记录；管理员订阅文件汇总和服务器历史使用该选择，用户历史及真实配额始终只使用协议计量。缺少已选网卡记录时提示汇总不完整。网桥、隧道和其底层接口可能重复经过同一份流量，选择时有说明。
- 当前开机计数、持久化账本与用户配额分别展示。即时速率使用相邻采集时间和同一 boot ID/ifindex，计数器变化或无可比样本时标记不可用。未指定接口时监控优先使用 IPv4 默认路由接口；没有默认路由标记时沿用常见虚拟接口排除规则，管理员可通过接口选择覆盖。
- 日历史显示基线中断和跨日间隔数量。跨日采集间隔的全部增量归入结束日并明确提示，没有根据插值猜测日界流量。没有报告的日期仍为未知，不等于零。离线且 Agent 仍运行时继续保留本地增量；Agent 停止期间同一内核计数器的差值在下一次采集补入，跨日则同样标记。
- VPS 全套 **58/58，通过，无跳过**，Windows **54 项通过、4 项在 VPS 实测**，三应用构建与类型检查通过。覆盖实际 Linux 内核网卡计数、持久化 Agent 重报/错误确认、HTTP/WSS 跨通道去重、接口选择、配额隔离、数据库失败回滚与修复重试。加密恢复验证网卡选择、累计、日账本及去重记录。
- 浏览器使用回环 API、实际控制器和独立 SQLite，验证未选择网卡拒绝保存、单/多接口汇总、用户计量隔离、基线与跨日提示、手机布局及断连保留/错误聚焦。测试夹具绕过生产管理员鉴权，受控数据由真实计量服务写入；内核计数的真实采集另在 VPS 验证。
- 系统网卡账本已补齐基础路径；精确日界分摊、服务器周期重置/基线/校准、节点或入站维度、跨服务归因、保留策略与大规模性能仍待补齐。当前不把其他服务流量误记为用户配额，也不宣称网卡选择能自动去重虚拟网络层。冻结 `0.2.0` 安装包尚不含这批源码。

## 协议流量日账本

对应[流量统计口径](https://miaomiaowux.com/docs/traffic-accounting/)中的日历史与冻结计费。当前新增的是协议用户统计，不是主机网卡账本。

- 新增 `20260908090000_daily_traffic` 迁移。按服务器、UTC 日期及用户保存原始上下行、原策略计费、实际写入配额、缺少策略和未进入配额的原始流量；字节以十进制字符串保存和传输。用户重置及倍率修改不会重算已保存历史。旧周期延迟上报保留原策略金额，但不扣新周期配额。
- Agent 将采集时间连同会话、序号持久化，重启或失败重试不改变日期。采集时间早于策略或超前超过容差时按接收日期记账，旧批次缺少时间也按接收日期归档，并在查询中说明。上报原始时间保留；日账本、服务器累计、用户配额和去重确认在同一事务提交。
- 管理员接口 `GET /api/traffic/history/options` 和 `GET /api/traffic/history` 支持服务器、用户、连续 1–90 天筛选，最多读取 50,000 行；超出时要求缩小范围。HeroUI 新增“流量统计”入口、日期快捷查询、每日条形展示及服务器明细。无记录显示“无上报”，真实零值显示零；有记录的服务器数量不代表全天连续覆盖。
- 用户历史按采集策略计费，另列实际配额入账；服务器区间展示按当前服务器方向计算。较大值模式先合并每台服务器区间上下行，每日图条仍展示原始上下行之和，不能把每日较大值相加当作区间值。
- VPS 全套 **54/54，通过，无跳过**；Windows **51 项通过、3 项在 VPS 实测**，三应用构建与类型检查通过。新增真实 Agent 进程重启/重报、时间归档、冻结倍率、旧周期补报、无策略流量、缺失日期和事务失败回滚用例；原加密恢复用例确认日账本原始量、计费量及报告数恢复。
- 浏览器通过本地实际控制器与独立 SQLite 检查服务器/用户筛选、7 天快捷查询、无记录/真实零值、断连保留与错误聚焦，以及 390px 视口无横向溢出。夹具直接调用真实计量服务生成测试账本，绕过生产管理员鉴权，仅用于交互验证。
- 本轮源码尚未纳入冻结的 `0.2.0` 发行包。网卡日账本、服务器周期重置/基线/校准、节点或入站维度、跨服务归因、保留策略与大规模性能仍待补齐。删除服务器会级联删除其日账本；删除用户不删除数字账本，但当前筛选下拉框只列现有用户，历史用户需以 ID 查询 API。

## 服务器协议汇总与外部流量

对应[流量统计口径](https://miaomiaowux.com/docs/traffic-accounting/)中订阅文件的管理员汇总与外部来源方向。

- 服务器保存协议用户上报的原始上传/下载累计和统计起始时间，与批次去重在同一事务中更新。旧权益代次的延迟上报仍属于服务器流量，但不进入已重置的用户配额；用户重置不清除服务器累计。首次上报前显示未知，不回填或猜测历史。
- 新管理员接口 `PUT /api/servers/:id/traffic-settings` 保存双向、上传、下载、较大值及展示容量，检查设置版本。HeroUI 服务器详情支持编辑、取消和错误保留。该方向只重算管理员展示，不改变采集时冻结的用户计费倍率；容量不执行服务器断流。
- 订阅文件可选择统计服务器，空选择汇总全部。文件标签命中的启用外部来源按来源方向加入管理员视图；各来源只累计一次。成员私有页及用户配额保持独立，不加入其他用户或上游流量。
- 外部同步解析 HTTPS `subscription-userinfo` 的 upload/download/total/expire，字节保持十进制字符串。畸形、重复字段和超长头被拒绝；失败保留上次有效节点与流量并标记旧快照，成功缺少流量头则标记未知。地址变化及错过自动同步窗口也提示旧快照。
- HeroUI 显示上游报送时间、方向、容量，管理员明细区分已知有限容量、不限量、未知与旧快照。选择无上报服务器时，折叠标题也标明“不完整”。文件展示额度仍可覆盖展示容量，真实套餐判断不变。
- VPS 全套 **50/50，通过，无跳过**；Windows **47 项通过、3 项在 VPS 实测**。真实 HTTPS 测试覆盖流量头精度、畸形拒绝、失败保留和缺失头；SQLite 覆盖服务器筛选、方向、批次去重、旧代次、套餐隔离及服务器原始累计恢复。追加 Windows 恢复用例验证文件服务器选择与上游流量/方向/状态保持一致。
- 浏览器通过实际控制器与独立 SQLite 检查服务器规则保存、文件选择/标签、上游方向变化、汇总明细、未知提示和 390px 手机布局；上游 UI 数据为明确的测试快照，真实网络获取在 VPS 验证。三应用构建与类型检查通过。
- 新迁移 `20260908080000_traffic_display` 尚未打入冻结安装包。当前只覆盖统计启用后的协议用户累计；服务器网卡账本、周期重置/基线/校准、日趋势、节点与用户明细和跨服务归因仍未齐备，不视为完整流量模块已经完成。

## 订阅短链与展示额度

对应[订阅文件文档](https://miaomiaowux.com/docs/subscribe-files/)的短链和文件显示限额。

- 管理员可为文件设置 12–64 位别名，HeroUI 提供随机生成、路径预览、校验和保存回读。别名以 `~` 前缀进入既有订阅/私有页路径，与随机令牌分开解析；订阅内容、二维码和客户端入口均使用真实渲染器。
- SQLite 独立保存别名哈希、加密原值和文件关联。更换、清空、重置及删除会停用旧别名，保留不可复用的哈希；版本冲突或别名冲突在事务内回滚。重置同时轮换随机令牌并清除别名，主订阅撤销后编辑文件不能恢复访问。只更换别名不会轮换原随机令牌，撤销全部旧入口使用“重置链接”。
- 文件展示额度为十进制字节字符串，留空沿用套餐、0 展示不限。客户端 `subscription-userinfo` 的 total 使用展示值，私有页同时说明实际套餐额度；真实计量、到期、节点授权和 HWID 限制保持按用户套餐执行。没有新增普通用户账户或重复计量。
- VPS 全套 **48/48，通过，无跳过**；Windows **45 项通过、3 项在 VPS 实测**。覆盖别名加密、冲突回滚、撤销与永久保留、超额不被展示覆盖绕过、大整数字符串、共享 HWID、节点交集及加密恢复后保留别名/展示值；真实 Mihomo 与 sing-box 使用别名解析出的配置完成连接回归。
- HeroUI 使用实际控制器与独立 SQLite 验证随机生成、无效输入保留、保存与私有页、重置后旧链接拒绝、旧别名不可复用、清空展示覆盖、中文断连提示及手机无横向溢出。UI 夹具绕过生产管理员鉴权，不等于公网客户端 GUI 验收。构建、类型检查与 VPS 日志见 [验收记录](deploy/VPS-ACCEPTANCE.md)。
- 新迁移为 `20260908070000_subscription_aliases`。本轮源码尚未打入冻结的 `0.2.0` 发行包。统计服务器筛选、外部来源流量头聚合、其他客户端格式等仍在后续清单。

## 网站文件与多域名

- 网站支持主域名及最多 19 个别名，统一校验重复、跨网站冲突和证书覆盖范围；保存仍为草稿，Agent 确认发布后才更新实际配置。真实 Nginx 已验证别名路由及 CSS MIME 类型。
- HeroUI 文件管理连接已应用静态目录，支持分页浏览、新建目录、文本编辑、上传/下载、文件重命名与文件/空目录删除。单文件上限 1 MiB；暂不支持大文件分块、目录重命名及递归删除，单目录最多 10,000 项、每页 200 项。
- 文件任务与网站发布互斥并校验部署版本；保存、重命名和删除使用内容版本检查，外部修改时拒绝覆盖。Agent 拒绝越界路径、符号链接、硬链接及特殊文件，Linux 使用目录句柄约束路径。新文件/目录在严格 umask 下仍可由 Nginx 读取，覆盖保留已有属主和权限。
- 文件内容保存在加密任务参数中，通用任务列表/SSE 不返回内容，管理员显式读取结果；Agent 在主控确认前持久化重报内容，确认后从任务日志删除内容。普通操作日志只显示元数据。
- VPS 全套 **46/46，通过，无跳过**；追加严格权限检查后文件专项 **4/4** 通过。Windows **43 项通过、3 项由 VPS 实测**。三应用类型检查和构建通过。浏览器连接实际控制器、独立 SQLite 和本地 Agent 文件执行器，检查真实保存、并发冲突保留、目录导航、新建文本、多域名草稿、断连保留及 390px 手机布局。该 UI 夹具不是生产鉴权或公网浏览器至 VPS 的完整链路验收。
- 本次为 `0.2.0` 冻结发布后的源码改动，尚未重建发行包；原安装包及其校验值保持不变。详细日志和环境见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。

## 0.2.0 发布与部署

- 当前编辑器、订阅模板、独立文件和持久化任务已纳入本仓库 `0.2.0` 构建。根版本统一生成主控、Agent 和恢复工具的版本；旧版本构建不能直接包装成新版本，版本不匹配的备份不能覆盖当前数据库。
- Debian 12 / amd64 上从完整源码构建，42 项测试全部通过。新版 Docker 完成管理员注册、Agent 握手、六协议实际下载、配置失败回退、AnyTLS 禁用/超额/到期及恢复、订阅和加密备份恢复。
- systemd 完成主控与 Agent 的 `0.1.0 → 0.2.0 → 0.1.0` 升级回退、新版全新安装和重启、保留数据重装、同版本不同产物拒绝。裸机恢复执行停写、预备份和健康检查，启动失败会恢复之前的数据库。Docker 内 Agent 升级监督、校验失败和进程中断恢复也已实测。
- 修复卸载保留目录后安装器找不到服务而退出，以及无法重装已停止的同版本的问题。运行中的同版本保持不重复安装；重装不把上一版本指针改成当前版本。
- 产物位置、校验文件和完整日志见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。原 `0.1.0` 冻结归档保留。公开发行渠道、其他系统/架构、真实 DNS/远端存储及客户端 GUI 等缺口仍按下表跟进。

## 最新验收：主控任务中断恢复

- 备份、定时备份/远端同步、ACME 申请和续期进入加密参数的 SQLite 持久化队列。资源互斥、执行租约、过期提交保护、最多三次中断恢复和手动重试已实现；重复重试同一失败任务只创建一个后续任务。远程 Agent 任务保持独立处理。
- 备份以固定标识恢复，临时文件同步到磁盘后原子发布；进程在文件完成后被强制终止，新进程复用原文件，不生成重复备份。已结束任务的临时 SQLite、部分归档和 ACME 工作目录会清理，活动任务及无关文件保留。
- ACME 未保存证书前中断时不自动重发签发请求；证书与恢复检查点一起提交，保存后只补服务下发。Agent 下发任务与检查点在同一事务创建，旧租约无法再发布配置或网站。证书下发排队成功不等于 Agent 已确认生效。
- HeroUI 任务页显示中文状态、执行次数、重试来源、证书保存阶段和失败重试；断连保留记录并聚焦错误。Windows 构建/类型检查通过，39 项测试通过；VPS 全套 **42/42，通过，无跳过**，含真实进程强制终止、租约抢占、重复重试和原业务回归。
- 外部来源继续使用已有 60 秒任务截止、版本保护与重新同步流程，此轮未迁入新队列。真实 DNS ACME、WebDAV/S3 服务端、长时间大规模并发及断电持久性仍需专项验收。上述实现现已纳入 `0.2.0` 归档。

## V3 协议筛选与中转验收

- `include-type` 支持不区分大小写及 ss/shadowsocks、hy2/hysteria2 等别名，和 `exclude-type` 组合；动态集合转换为 Mihomo 1.19.30 的 AdapterType 排除表。未识别的输入类型拒绝保存；未来内核新增类型需更新能力表，不能据此保证任意版本的行为。
- `dialer-proxy-group` 转换为独立 inline 节点集合或外部集合副本，`relay` 转为 select；原节点保持原连接方式，HTTP 集合副本使用独立缓存路径。冻结 include-all-providers 的原始集合范围，避免新生成集合被入口重新包含。名称正则由客户端执行；空组默认 REJECT，中转空组强制 REJECT。
- HeroUI 支持协议包含、中转组及中转类型选择；重命名同步引用，删除引用保护，循环引用保留草稿并报错；预览显示生成集合。桌面与 390×844 手机表单已操作验证。
- VPS 全套 **36/36，通过，无跳过**。真实 Mihomo 连接测试覆盖受管节点中转、HTTP provider 中转、SS 别名筛选、空筛选、原节点独立使用；停止入口后两个中转组均失败，原节点仍成功。六协议、AnyTLS 计量与撤权、备份和 Nginx 一并回归。
- 该验收不包含所有中转协议组合、UDP 链路或客户端 GUI。若同一流量经过两个计费的受管服务，尚未新增链路身份关联去重；当前仍按各服务协议增量计量。模板声明的外部 provider 不纳入本面板用量统计和凭据撤权。V3 和编辑器现已纳入 `0.2.0`，原 `0.1.0` 冻结包保留。

转换依据：[妙妙屋 X 模板字段](https://miaomiaowux.com/docs/templates/)、[Mihomo dialer-proxy](https://wiki.metacubex.one/en/config/proxies/dialer-proxy/)、[Mihomo v1.19.30 AdapterType](https://github.com/MetaCubeX/mihomo/blob/v1.19.30/constant/adapters.go)。

当前目标已扩展为文档功能对齐及 HeroUI 交互优化。`IMPLEMENTATION.md` 中首轮排除项是历史交付边界；下表仍保留这些功能为后续工作，不将其视为已完成或永久排除。旧实例兼容、支付与普通用户账户等涉及最初明确边界的事项，实施前仍需按具体需求处理。

## 对照清单

| 文档领域 | 当前实现与证据 | 下一步或验收缺口 |
| --- | --- | --- |
| 直接安装、Docker、Agent、版本更新 | 本仓库构建；固定版本与校验；安装、升级、监督回退；Debian 12 amd64 VPS 已测 | Debian 13、Ubuntu、arm64；公开发行包及下载渠道 |
| Cloudflare Tunnel、新手教程、迁移 | HeroUI 本地 Tunnel 发布向导、离线配置工具、独立域名与 HTTP/WS 隔离、两种部署文件；固定 cloudflared 规则已测 | 正式 DNS/Tunnel 公网接入、远程管理 Token 模式、完整服务生命周期；迁移需与全新部署边界区分 |
| 远程服务器、服务管理 | 登记、主机指标、任务、日志、重启与升级；HeroUI PTY；显式 HTTPS 直连和自动回退复用任务/计量/终端、独立凭据和连接状态；VPS 已测 | 更完整详情交互；最新 PTY/自动连接包完整升级生命周期 |
| Nginx 网站 | HeroUI 草稿/编辑/历史回退、静态/代理/WS、多域名、端口、证书、日志；已应用目录文件管理；真实 Nginx 与文件安全检查通过 | 大文件分块、目录重命名、更多专用代理选项；公网访问验收；新文件功能待打包 |
| Xray 入站与协议矩阵 | 六协议、HeroUI 新建/编辑/启停、标签、证书、Vision/REALITY、Host/心跳/ALPN/XHTTP 模式、JSON、发布及失败回退；六协议与 TLS WS/XHTTP 真实下载 | 全协议传输组合与目标客户端矩阵；入站高级参数可视化继续完善 |
| Xray 出站 | HeroUI CRUD、排序、凭据/TLS、JSON、引用保护；WireGuard 多 peer 表单、用户空间 TCP/UDP 实流量和用户计量已在 Windows/Linux 验证 | WARP 注册、账户/许可、双栈管理；WireGuard 公网验收及其他专用协议表单 |
| Xray 路由 | 规则 CRUD、排序、入站筛选、覆盖告警；四种均衡策略；后台观测与 leastPing 故障切换、备用及恢复已实测 | 更多快捷规则、复杂条件；突发观测和 leastLoad 实流量、完整协议/UDP 矩阵 |
| Xray 系统配置 | 日志、结构化 DNS/Hosts、路由策略及 JSON；策略等级/超时/缓冲区/统计、两种连接观测 HeroUI 表单；管理 API 固定保护；UDP DNS 和后台观测 VPS 已测 | DNS 全运输/并行/过期缓存与资源规则、更多日志配置；全部策略超时行为、在线统计展示与多版本能力矩阵 |
| AnyTLS、Snell | AnyTLS 独立 sing-box、凭据、计量及撤权；VPS 禁用/到期/超额通过 | Snell 尚未接入；新增内核需同样实现生命周期、统计与撤权 |
| 节点管理、生成订阅 | 节点编辑/排序/标签、二维码、URI、Mihomo/sing-box 渲染；外部源导入 | 目标客户端真实应用导入；公网链路复测；完整能力版本表 |
| 订阅文件、模板 | 外部来源、短链、展示额度、统计服务器与标签命中的上游流量；命名模板、授权子集、V3 筛选/中转和真实客户端连接 | 更多格式与完整内核校验；完整中转/客户端矩阵；新功能待打包 |
| 流量统计口径 | 协议/网卡分离、去重、大整数、冻结计费；UTC 日账本、接口选择、用户/服务器历史、月周期、校准及基线；服务器/文件管理员汇总、首页容量与 30 日趋势；VPS 已测 | 精确日界分摊、任意账期/时区、节点或入站维度；跨服务归因、保留策略、大规模性能与配额同步窗口 |
| 节点测速、节点限速 | 基础 TCP 延迟任务 | 吞吐测速和实际限速未新增；不能将延迟或套餐额度等同于它们 |
| 分享服务器、内嵌 Xray | 显式共享配置模板；独立管理 Agent | 跨面板服务器共享、主控内嵌协议进程未实现 |
| 系统设置、自定义 CSS | 中文导航、深浅色、移动布局、订阅和备份设置 | 文档中的全部系统项待逐项核对；CSS 管理入口未新增 |
| 证书 | 手动上传、三家 DNS ACME、续期、加密、下发；VPS 手动证书通过 | 真实 Cloudflare/阿里云/DNSPod 签发及自动续期，需要实际 DNS 授权资源 |
| 用户与套餐 | 单有效套餐、权益快照、更新显式同步、流量/有效期、撤销和 HWID | 文档中的多套餐等差异需单独落地；客户端设备行为验收 |
| 自定义规则、路由出站 | 独立规则集持久化、HeroUI CRUD/启停/排序、双格式预览；模板选择与文件继承/覆盖；关联保护与加密恢复；Mihomo/sing-box 实际策略与 GEOIP 加载通过 | 规则资源导入与复用、更多格式与完整模板交互继续核对 |
| 备份恢复、定时同步 | 加密一致性快照、定时/保留、离线恢复；VPS 实际 MinIO S3/Nginx WebDAV 认证回读、解密与保留通过；持久化任务及备份进程中断恢复通过 | 更多云存储服务商；容量、并发与版本迁移策略 |
| 外置探针、探针 API | Agent 自带主机指标 | 外置探针服务、独立接口契约与部署未实现 |
| MCP、Telegram、Turnstile | 原有高级入口保留；本轮未宣称这些功能完成 | 逐项设计鉴权边界、接口与界面，真实集成验收 |
| 帮助、FAQ、拼车及版本记录 | 部署说明、实现记录、VPS 验收说明 | Nodify 专属教程、错误排查、客户端步骤与帮助导航 |

## 本轮配置编辑交付

对应[出站](https://miaomiaowux.com/docs/xray-outbounds/)、[路由](https://miaomiaowux.com/docs/xray-routing/)、[系统配置](https://miaomiaowux.com/docs/xray-system-config/)三组功能。

- 新组件直接使用 HeroUI；列表和表单在桌面并排，手机先显示编辑表单。
- 每个条目先写入本地草稿，再保存/发布整个版本。编辑中禁止跨配置分类、切换完整 JSON 或保存未写入条目；未保存修改时禁止发布旧版本，并提供恢复已保存草稿。
- JSON 结构错误保持可编辑并阻止切回表单，避免错误数组/对象导致崩溃。修改 DNS 简单服务器时保留高级对象和其他字段。
- 统一契约拒绝失效的出站、均衡和入站引用以及前置代理循环；重命名同步更新精确引用。出站被均衡前缀引用时要求先调整前缀，避免改变匹配含义。
- 共享模板发布到服务器时，路由中的入站标识随生成的服务器节点 ID 一起映射。
- 最低延迟、最低负载以及设置备用出站的策略自动合并连接观测配置，保留用户已有观测参数。
- 修正浅色模式下 HeroUI 下拉框、菜单和选中标签的颜色；补齐标签与面板的无障碍关联，手机标签保持单行并可横向滚动。

本地检查：三应用类型检查及构建通过；完整测试 18/18，通过真实内核执行，无跳过。浏览器已检查出站重命名/删除保护、协议选择、必填校验、均衡观测生成、DNS 保留、错误 JSON、规则顺序和手机视口。VPS 结果及产物范围见 [VPS-ACCEPTANCE.md](deploy/VPS-ACCEPTANCE.md)。这些证据不代表上表全部功能完成。

## 后续优先顺序

1. 继续完善服务器详情、入站高级参数与网站大文件操作，避免 JSON 成为常用操作的必经步骤。
2. 补齐 WARP、规则资源、订阅文件等核心管理差异，并通过真实发布与客户端验收。
3. 落地测速/限速、探针、Snell、共享、MCP/机器人等新增领域；每项同步交付鉴权、持久化、UI、部署和实际测试。
4. 完成公网客户端、DNS、远端备份以及多发行版/架构验证，最后核对文档全表。

## 入站编辑后续交付

- 入站支持创建、编辑、启停和删除；编辑保留 ID、套餐/路由关联与 `extra` 高级参数。引用未解除时拒绝删除，AnyTLS 逻辑规则中的嵌套引用同样检查。
- HeroUI 向导按协议和传输筛选安全层，支持证书绑定、SNI、路径、Shadowsocks 算法、Vision、REALITY、TCP/UDP 转发目标和标签；JSON 模式可编辑额外参数。
- 浏览器使用 Web Crypto 生成真实 X25519 密钥及随机 shortId；预览隐藏私钥。校验密钥长度、shortId 偶数长度和 REALITY 服务域名。尚未据此声称所有 REALITY 客户端组合均已通过连接测试。
- 入站与出站编辑互斥，未写入条目时禁止保存整个草稿。加载配置失败显示重试，保存/提交发布期间锁定编辑，避免并发修改被返回请求覆盖。
- SQLite 回归验证编辑在 Agent 确认前不改变节点名称和端口，确认后更新且保持节点 UUID，启停会同步节点状态。
- Windows 与 VPS 完整测试 **20/20，通过，无跳过**。浏览器验证编辑保留字段、删除引用保护、ID 不可变、密钥生成与预览、AnyTLS 证书必填和新增、禁用及手机布局。三应用类型检查与构建通过；新增前端仍未打入 VPS 发布归档。

## 网站管理后续交付

对应[网站管理文档](https://miaomiaowux.com/docs/website-management/)。

- HeroUI 网站表单支持静态目录、反向代理、WebSocket、自定义 HTTP/HTTPS 端口、证书绑定和 HTTPS 跳转。输入会拒绝配置注入、带凭据的代理 URL、证书域名不匹配及 HTTP/TLS 端口冲突。
- SQLite 保存网站草稿、历史版本、发布序号、最后已确认配置与关联任务。保存不会发布；只有当前任务经 Agent 确认后才更新已应用版本，失败保留原配置，超时后可以重试。
- 编辑、发布历史版本、删除和按站点日志均使用真实 API/任务。删除等待确认并保留静态文件与上游应用；Agent 持久化删除标记，拒绝旧发布任务重放。
- 证书续期及 Agent 状态丢失后，重新下发最后已应用版本，避免意外发布尚未确认的草稿。证书私钥只出现在加密任务和 Agent 专属文件中，普通网站和任务列表不返回它。
- Nginx 发布先测试候选配置和端口，再启用并持久化；失败恢复旧配置和旧证书。启动时从已持久化记录重建配置，恢复发布中断；没有启用网站时不启动 Nginx。仅管理 Nodify 专属目录和进程。
- VPS 完整测试 **22/22，通过，无跳过**；追加 HTTPS 与中断恢复后，网站专项 **2/2** 再次通过。覆盖静态访问、TLS 校验、308 跳转、错误证书保留、代理大请求体、WebSocket、日志、启动失败回退、重启恢复和删除重放。
- 本地类型检查、三应用构建通过；本地测试 21 项通过，真实 Nginx 项由 VPS 执行。浏览器使用真实隔离 SQLite，验证草稿编辑/历史、实际发布与删除排队、输入错误、断网保留记录和手机无横向溢出；该 UI 检查未连接 Agent，不代表浏览器至 VPS 的完整链路已通过。
- 测试页面和进程已关闭，临时容器已自动移除。最新源码已同步到 VPS 测试目录；冻结的发布归档仍不包含这些后续改动。

## 自定义分流规则后续交付

对应[自定义规则文档](https://miaomiaowux.com/docs/custom-rules/)。

- 新增 `NodifyRuleSet` 及 SQLite 迁移，管理员通过 `/api/rule-sets` 创建、编辑、启停、排序、删除及预览。更新和删除检查版本，排序检查完整列表与版本，防止旧页面覆盖新修改。全局最多 100 个规则集、10,000 条规则。
- HeroUI 支持类型/匹配值/策略向导、批量粘贴、逐行校验、编辑顺序、双格式预览和删除确认。尚未追加的向导输入阻止保存；编辑期间锁定其他修改操作，服务端冲突保留编辑内容。
- 支持 DOMAIN、DOMAIN-SUFFIX、DOMAIN-KEYWORD、IPv4/IPv6 CIDR 和 GEOIP；策略为 PROXY、DIRECT、REJECT。启用规则按持久化顺序放在模板规则之前；当前应用于所有有效用户的配置订阅，下一次客户端刷新时生效。URI/Base64 节点列表不携带路由规则。
- Mihomo 的 PROXY 指向 Nodify 代理组；sing-box 生成对应路由动作与独立直连出站。节点名称避开代理组和系统出站名称，缺少默认组时补齐。到期或没有可用节点的 sing-box 订阅仍拒绝连接，不因直连规则放行。
- sing-box 的 GEOIP 转为 SagerNet 远端二进制规则集，同一国家只生成一份资源；客户端需能访问该地址。Mihomo 使用自己的 GeoIP 数据。已验证 sing-box 1.14.0 与 Mihomo 1.19.30，未将此结果扩展到所有旧版客户端；`download_detour` 在 sing-box 1.16 移除前需迁移到新的 HTTP Client 配置。
- VPS 完整测试 **25/25，通过，无跳过**，包含真实 GEOIP 下载/加载、DIRECT/PROXY/REJECT 分流、停掉代理后仅直连仍可用、SQLite 并发/优先级/启停/删除、到期拒绝与加密备份恢复。Windows 完整测试 24 项通过，Nginx 项在 VPS 执行；本地规则真实连接测试使用 sing-box。
- 浏览器通过真实隔离 SQLite 检查新建、追加/粘贴校验、预览、禁用、排序、编辑保留、并发冲突、删除、断网保留记录和手机无横向溢出。三应用构建与类型检查通过。测试入口和进程已关闭；发布归档尚未更新。

## 外部订阅来源后续交付

对应[订阅文件文档](https://miaomiaowux.com/docs/subscribe-files/)中的外部来源管理。

- 来源及节点设置持久化到 SQLite，支持名称、HTTPS 地址、启停、标签、手动/定时同步和删除。同步返回真实任务 ID，更新和删除检查版本及运行中的任务；过期任务不能覆盖新配置。
- URI、Base64 和 Mihomo YAML 导入按配置生成稳定节点 ID，忽略名称和 JSON 字段顺序变化；本地名称、启停和标签在上游重命名后保留。凭据或其他配置变化视为新节点，套餐须通过新 ID 或明确授权的标签重新选取。
- 来源 URL 加密存储，普通列表只显示地址主机和节点元数据，不返回 URL 密钥或协议密码。请求保持 TLS 验证、拒绝本地地址和重定向，限制为 15 秒、5 MiB；解析失败保留上次有效节点和成功时间。最多 100 个来源，单个节点结构限制为 64 KiB。
- 启用的外部节点只有在套餐明确授权 ID 或标签时才分发。外部用量和凭据撤销仍由原提供方管理。来源设置及节点覆盖已纳入加密备份恢复测试。
- HeroUI 支持编辑保留地址、手动同步进度、节点覆盖、名称/地址/标签筛选、每页 50 个节点及删除确认。浏览器通过 SSH 转发连接 VPS 的独立 SQLite 和真实 HTTPS 来源，完成成功/失败保留、重命名保留、禁用、55 节点分页、标签筛选、空 URL 编辑、取消/确认删除、断连错误及手机布局检查。
- VPS 最新全套 **28/28，通过，无跳过**，包括不可信证书拒绝、15 秒超时、重定向和大小限制；Windows 26 项通过，Nginx 与真实 HTTPS 项在 VPS 验证。类型检查与三应用构建通过。临时 UI、隧道及测试容器已关闭，原有服务 PID 未变。
- 本地同步任务中断后依靠超时解锁和重试，尚不自动恢复正在执行的下载。独立订阅文件、模板绑定和更多客户端组合仍待完善；冻结发布归档尚未包含这些改动。

## 命名订阅模板后续交付

对应[模板管理文档](https://miaomiaowux.com/docs/templates/)及 [Mihomo 代理组配置](https://wiki.metacubex.one/en/config/proxy-groups/)。

- 新增命名双格式模板实体和迁移，管理员通过 `/api/subscription-templates` 创建、编辑、删除、导入解析、预览和切换默认项。保存与默认选择均检查版本，默认模板不能直接删除；复制建立独立条目，最多 100 个模板。
- 系统设置中的 HeroUI 模板管理提供命名、复制、YAML/JSON 粘贴与文件读取、完整 JSON、代理组表单。支持 select、url-test、fallback、load-balance、动态包含节点/代理集合、客户端筛选/排除、探测地址/间隔/容差及隐藏。未知高级字段在表单编辑后保留；代理组重命名同步成员与规则策略，存在引用时阻止删除。
- 未写入的代理组和导入内容会阻止保存。格式错误与并发冲突保留输入；保存失败自动定位错误，避免长表单底部看不到提示。列表请求失败保留已有记录。默认项切换说明所有用户的影响并确认；URI 列表不应用配置模板。
- 模板与实际订阅共用占位符展开逻辑，支持 `$NODES` / `__PROXY_NODES__` 和 use 中的 `__PROXY_PROVIDERS__`。不存在于本次订阅的节点成员会被跳过，普通规则的缺失策略替换为 REJECT 并在预览提示；没有节点的普通组使用 REJECT。正则由客户端执行，预览不冒充客户端过滤、远端资源下载或完整配置校验。
- VPS 全套 **30/30，通过，无跳过**，覆盖实际默认模板订阅、四种代理组的 Mihomo 加载、分流访问、sing-box 模板字段、到期拒绝、并发版本及默认删除保护，以及加密备份恢复后的模板和选择项。Windows 28 项通过，真实 Nginx 与 HTTPS 来源两项在 VPS 执行。
- 浏览器使用独立 SQLite 和生产控制器方法验证新建/保存/复制、默认项确认、引用删除保护、组重命名、逗号输入、展开预览、导入错误保留、未知字段保留、错误 JSON、并发冲突保留、删除取消、断连保留列表和手机无横向溢出。
- 尚未实现按用户/独立订阅文件绑定模板、V3 的 include-type / dialer-proxy-group 转换、完整规则资源表单与全部客户端格式；暂不支持的扩展明确拒绝导入。当前默认项作用于所有用户，原订阅设置可复制为命名模板或切回。冻结发布归档仍待更新。

## 独立订阅文件后续交付

对应[订阅文件](https://miaomiaowux.com/docs/subscribe-files/)与[生成订阅](https://miaomiaowux.com/docs/generator/)中的独立分发入口，补齐上一节的文件绑定缺口。

- 新增 `NodifySubscriptionFile`、规则关联实体及迁移，提供管理员 `/api/subscription-files` 的资源选项、列表、新建、编辑、启停、重置链接与删除。每位用户最多 50 个文件；所属用户创建后不可转移，编辑、重置和删除检查版本。
- 文件可绑定命名模板或跟随系统默认，选择全部/指定节点及标签、全部/指定/不附加自定义规则集。来自主控节点和外部来源的节点始终与用户当前套餐授权取交集；空选择不分发节点，标签不能授予套餐之外的节点。自定义规则保留全局顺序并排在模板规则之前；选择“不附加”不移除模板自带规则。
- 每个文件持有加密存储的随机私有链接，主订阅和文件链接复用实际渲染器。文件保存用户主订阅哈希，主订阅撤销后所有旧文件链接失效；普通编辑不能解除失效，管理员必须显式重置文件链接。模板及选中规则集存在文件引用时，数据库拒绝删除。
- 文件共享用户协议凭据、用量及 HWID 限额，不为每个文件重复计量或创建额外设备额度。文件停用/到期/重置仅控制链接分发，撤销已导入的协议访问仍通过用户停用或主订阅撤销。用户本身到期/超额的协议撤权机制继续执行；文件不能延长用户有效期。
- HeroUI 位于“节点与订阅”，支持资源加载错误、用户与授权节点选择、节点查找/分页、规则绑定、编辑保留、版本冲突定位、确认重置/删除、状态和有效到期显示。模板列表显示引用文件数量，默认模板提示改为作用于主订阅及未单独绑定的文件。
- 私有页显示文件名、实际用量/到期、节点和独立链接的二维码/客户端入口。小额度自动选择 B/KiB/MiB 等单位；停用时隐藏导入和下载入口，剪贴板失败显示错误。没有新增普通用户账户系统。
- VPS 全套 **34/34，通过，无跳过**：真实 Mihomo/sing-box 直接使用文件链接对应的订阅配置完成分流访问；SQLite 验证套餐交集、外部标签授权、绑定优先级、文件与用户到期/额度、主撤销/文件轮换、HWID 跨文件限制、关联删除保护和加密备份恢复。Windows 32 项通过，另外两项在 VPS 实测。
- 浏览器连接独立 SQLite 和生产控制器，完成创建/编辑资源选择、私有页/二维码/客户端链接、停用为空、旧链接重置失效、新链接可用、并发冲突保留、删除取消、断连保留及两个页面 390px 视口无横向溢出。该证据不等于公网客户端 GUI 导入验收。
- 自定义短链别名、文件展示额度覆盖、统计服务器筛选和 V3 扩展尚未补齐。模板中手工声明的外部代理集合仍按模板独立分发，不属于本面板节点/外部来源的选择与真实用量统计。冻结发布归档尚未更新。
