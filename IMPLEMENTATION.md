# Nodify 核心改造：实现与验收记录

> 本文为首轮历史快照。2026-09-09 的当前状态、剩余工作和接手步骤见 [HANDOFF.md](HANDOFF.md)，最新证据见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)。

更新于 2026-09-08。保留工作区原有未提交修改；没有清空数据库、迁移正在使用的实例或覆盖本机已有后端进程。此次迁移面向全新部署。

**五个阶段的主要代码、界面与部署工具已落地，Debian 12 / amd64 的主要业务流程已在 VPS 实测。其他系统架构、公网客户端和外部服务验收仍有缺口，不能据此标记整份方案完成。**

后续文档功能对齐及本轮 HeroUI 出站、路由、DNS 编辑器交付见 [FEATURE-PARITY.md](FEATURE-PARITY.md)。新配置编辑功能的本地和 VPS 完整测试均为 18/18，通过真实内核执行，无跳过。下文表格与测试数量保留原阶段的历史记录。

下表保留首轮交付快照；后续 VPS 实测、修复和剩余范围以 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md) 为准。

## 分阶段交付

| 阶段 | 本仓库实现 | 本次验证 | 尚待验证或补齐 |
| --- | --- | --- | --- |
| 1. 构建、部署、界面 | 根目录 setup/build/typecheck；本地契约；自建主控和 Agent 镜像；amd64/arm64 发布包及校验清单；systemd 安装/升级/回退/卸载；中文 HeroUI 导航、深浅色、手机布局 | Windows 三应用类型检查与构建通过；安装/管理脚本 bash 语法通过；桌面及 390px 浏览器检查 | 干净 Linux 构建、Docker 镜像构建及 systemd 全生命周期；新加批量升级与共享配置界面的最后一轮浏览器回归 |
| 2. 服务器与 Agent | 一次性令牌、会话登记、凭据轮换/撤销；WSS 与 HTTPS 长轮询；持久化任务和 Agent 日志；主机指标；独立升级监督进程；单台/批量升级入口 | 真实 HTTP/WebSocket 鉴权、重放、长轮询、跨服务器任务隔离、重复确认、轮换后旧连接关闭测试通过 | 真实公网断线、断电、升级中断及回退；旧 HTTP 高级节点保留，但新 Agent 未提供统一任务通道的 HTTP 直连监听器 |
| 3. 协议、网站、证书 | 六协议编译、独立 AnyTLS sing-box；表单与高级 JSON；显式共享模板；草稿/发布/确认/历史版本；TCP/UDP 指定目标转发；Nginx 静态/代理/WS；手动证书、三家 DNS ACME、续期下发与加密账户材料 | 配置能力/端口/字段保留测试；六协议真实内核下载；AnyTLS 两用户计量与额度撤权；重复发布与共享绑定测试通过 | 实际 Nginx 发布和回退；真实 ACME DNS 签发/续期；所有传输与 REALITY 组合；转发节点选择的界面回归 |
| 4. 节点、订阅、套餐 | 节点生成/编辑/排序/标签/分享二维码；外部 URI/Mihomo 导入与更新；套餐模板及权益快照；成员创建/禁用/续期/换套餐/重置/撤销/HWID；私有订阅页；Mihomo、sing-box、URI 渲染与基本兼容过滤 | SQLite 权益快照、旧倍率离线批次、周期代际隔离测试；外部源解析与内网地址拒绝；真实浏览器创建服务器/套餐/成员和页面交互；sing-box 客户端六协议下载 | Clash Verge、v2rayN/NG、Shadowrocket 实际客户端导入与连接；客户端完整版本能力矩阵；AnyTLS 到期的真实时钟端到端测试（已测策略函数）；外部源真实网络失败后的定时更新回归 |
| 5. 监控与备份 | 网卡与协议流量分离；BigInt 字节、方向/倍率、批次去重及落盘确认；缓存额度；概览异常入口/TCP 延迟任务；加密本地备份、周期保留、WebDAV/S3；离线校验/预备份/恢复工具 | 真实 SQLite 重复批次与大整数运算；加密备份还原用量；错误密码及损坏包拒绝；打开的 WAL 数据库拒绝回退、关闭后成功恢复测试通过 | WebDAV/S3 真机传输；全新 Linux 主控恢复后的管理员登录、证书、Agent 重连和持续计量；systemd 恢复后健康检查与自动回退实测；Docker 离线恢复后仍需操作员健康确认 |

## 已执行的检查

- `npm run typecheck`：后端、前端和保留的 NestJS 节点通过。
- `npm run build`：本地契约、Prisma 生成、Vite 前端、Rspack 后端和节点通过。后续后端修正另行重建。
- `npm test`：10 项通过、0 失败；真实内核用例默认因未指定二进制而跳过，已另行显式执行通过。
- `tests/engines.test.mjs`：使用 Xray 26.7.28 和启用统计的 sing-box 1.14.0 构建，VLESS、VMess、Trojan、Shadowsocks、Hysteria2、AnyTLS 各验证 65,536 字节下载；两个 AnyTLS 用户的流量分开统计，清除额度已耗尽用户后连接失败。
- `tests/deployment.test.mjs`：真实 SQLite WAL 快照及离线回退验证，不是 Linux 服务生命周期测试。
- 新 Agent、部署与发布脚本通过 `node --check`；安装和管理脚本通过 Git Bash `bash -n`。
- 浏览器检查使用隔离的临时 API/数据库；验证主要页面、浅色/深色、390px 视口和实际表单提交。临时测试入口已删除，测试服务已停止，生产源码没有鉴权绕过或模拟成功数据。

构建仍出现原有大包、浏览器外置 stream、wasm_exec 脚本和字体下载提示；没有将全面拆包或清理 Mantine 纳入此次改造。构建所需依赖、字体、Go 模块仍需要网络。

## 实现边界

- 新接口集中在 `apps/backend/src/modules/nodify`，共享校验在 `packages/nodify-contract`，新 Agent 在 `apps/node/agent`。旧功能入口仍保留；上游发行名用于原有包标识与版权，不再作为默认成品下载源。
- 新 Agent 管理自身子进程，systemd 管理其监督进程。Docker 内升级可以更换 Agent 与内核，更新监督进程/基础镜像仍需重建容器。
- 配置的用户凭据、入站、统计端口由系统管理；高级 JSON 对这些保留字段有明确约束，不能绕过套餐授权。其他受支持字段保留并交由真实内核验证。
- 终端当前是有超时和输出上限的命令任务，不是交互式 PTY；任务 SSE 提供状态和最终结果，不连续传输终端字符流。
- 证书签发、备份等主控本地长任务记录状态，但 API 重启后不会自动续跑进行中的进程；超时标记失败，保留旧资源，可重新提交。
- 用户配额按异步协议增量汇总，多 Agent 并发及网络中断存在同步窗口，不能保证在额度边界精确到最后一个字节断流。批次、任务日志和历史配置尚未实现长期归档压缩，需关注 SQLite 增长。
- 当前备份数据库上限 128 MiB；网站文件目录、Agent 宿主机文件和二进制不包含在主控备份中。恢复仅支持同版本且必须停止写入进程。
- 设备限制只约束携带 HWID 的订阅请求；外部节点只是订阅分发，面板无法撤销其服务端凭据或获得真实用户流量。
- 支付、普通用户登录、多套餐、Snell、跨面板共享、MCP、Telegram 机器人、外置探针、测速与限速没有新增。

## 后续 Linux 验收顺序

1. 在干净 Debian/Ubuntu 的 amd64 与 arm64 上构建镜像及裸机包，验证默认主控服务、Valkey、管理员初始化和 Nodify 前端版本。
2. 各接入一个 Docker 和 systemd Agent，测试 WSS、禁止 WS 后的长轮询、凭据轮换/撤销和服务器重启。
3. 发布六协议、Nginx 和证书；在目标客户端实际导入订阅并下载，覆盖禁用、真实到期、超额、断网与恢复。
4. 执行固定版本升级、校验失败、升级中断和回退；检查系统已有 Xray/Nginx 未被接管。
5. 将加密备份恢复到全新实例，验证登录、服务器认证、证书私钥、订阅与持续计量，再验证真实 WebDAV/S3 上传与保留策略。

已使用用户提供的 `185.99.135.224` 完成 Debian 12 / amd64 验收。Docker 六协议、计量与撤权、离线策略、备用通信、升级中断、加密恢复以及 systemd 生命周期的结果见 [VPS 验收记录](deploy/VPS-ACCEPTANCE.md)；公网直连及未覆盖的环境仍明确列为待验证。

操作命令和目录约定见 [deploy/README.md](deploy/README.md)。
